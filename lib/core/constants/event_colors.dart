import 'package:flutter/material.dart';

/// Fixed palette for event category color-tagging — deliberately a short,
/// curated list rather than a full color wheel: "a simple picker for a few
/// available colors to categorize events", not a custom theme builder.
const List<Color> eventCategoryColors = [
  Color(0xFF7C83FD), // indigo
  Color(0xFFFF6B6B), // coral
  Color(0xFFFFA94D), // orange
  Color(0xFFFFD43B), // yellow
  Color(0xFF69DB7C), // green
  Color(0xFF4DD4E8), // cyan
  Color(0xFFB197FC), // violet
  Color(0xFFF783AC), // pink
];
